if include_kttsd_festival
  festival_DIR = festival
endif

#if include_kttsd_festivalcs
#  festivalcs_DIR = festivalcs
#endif

#if include_kttsd_freetts
#  freetts_DIR = freetts
#endif

if include_kttsd_command
  command_DIR = command
endif

#SUBDIRS = $(festival_DIR) $(festivalcs_DIR) $(freetts_DIR) $(command_DIR)
SUBDIRS = $(festival_DIR) $(command_DIR)
